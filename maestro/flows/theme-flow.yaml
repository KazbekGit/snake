appId: com.socialstudies.app
name: Theme Flow Test
tags:
  - theme
  - dark-mode
  - light-mode

# Тест переключения темы
---
- launchApp
- runFlow:
    when:
      true: ${MAESTRO_FLOW_THEME == "true"}
    file: theme-flow.yaml

# Проверяем, что приложение запускается
---
- launchApp
- assertVisible: "Обществознание"

# Проверяем переключатель темы в TopNav
---
- tapOn:
    id: "theme-toggle"
    optional: true
- assertVisible: "Обществознание"

# Тест переключения темы на главном экране
---
- launchApp
- tapOn: "Обществознание"
- assertVisible: "Продолжить изучение"

# Находим и нажимаем переключатель темы (иконка солнца/луны)
---
- tapOn:
    text: "🌙"
    optional: true
- tapOn:
    text: "☀️"
    optional: true

# Проверяем, что тема изменилась (визуально)
---
- assertVisible: "Продолжить изучение"

# Переходим в статистику для проверки темы
---
- tapOn: "Статистика"
- assertVisible: "Статистика обучения"

# Проверяем переключатель темы в статистике
---
- tapOn:
    text: "🌙"
    optional: true
- tapOn:
    text: "☀️"
    optional: true

# Возвращаемся на главный экран
---
- tapOn: "← Назад"
- assertVisible: "Продолжить изучение"

# Проверяем, что тема сохранилась
---
- assertVisible: "Обществознание"
